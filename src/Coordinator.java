public class Coordinator extends Users{
    public Coordinator(String user_id, String fullName, String username, String password, String email, String securityQuestion) {
        super(user_id, fullName, username, password, email, securityQuestion);
    }
}
